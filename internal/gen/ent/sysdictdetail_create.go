// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wanhello/omgind/internal/gen/ent/sysdictdetail"
)

// SysDictDetailCreate is the builder for creating a SysDictDetail entity.
type SysDictDetailCreate struct {
	config
	mutation *SysDictDetailMutation
	hooks    []Hook
}

// Mutation returns the SysDictDetailMutation object of the builder.
func (sddc *SysDictDetailCreate) Mutation() *SysDictDetailMutation {
	return sddc.mutation
}

// Save creates the SysDictDetail in the database.
func (sddc *SysDictDetailCreate) Save(ctx context.Context) (*SysDictDetail, error) {
	var (
		err  error
		node *SysDictDetail
	)
	if len(sddc.hooks) == 0 {
		if err = sddc.check(); err != nil {
			return nil, err
		}
		node, err = sddc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SysDictDetailMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sddc.check(); err != nil {
				return nil, err
			}
			sddc.mutation = mutation
			node, err = sddc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(sddc.hooks) - 1; i >= 0; i-- {
			mut = sddc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sddc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sddc *SysDictDetailCreate) SaveX(ctx context.Context) *SysDictDetail {
	v, err := sddc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (sddc *SysDictDetailCreate) check() error {
	return nil
}

func (sddc *SysDictDetailCreate) sqlSave(ctx context.Context) (*SysDictDetail, error) {
	_node, _spec := sddc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sddc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sddc *SysDictDetailCreate) createSpec() (*SysDictDetail, *sqlgraph.CreateSpec) {
	var (
		_node = &SysDictDetail{config: sddc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: sysdictdetail.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sysdictdetail.FieldID,
			},
		}
	)
	return _node, _spec
}

// SysDictDetailCreateBulk is the builder for creating many SysDictDetail entities in bulk.
type SysDictDetailCreateBulk struct {
	config
	builders []*SysDictDetailCreate
}

// Save creates the SysDictDetail entities in the database.
func (sddcb *SysDictDetailCreateBulk) Save(ctx context.Context) ([]*SysDictDetail, error) {
	specs := make([]*sqlgraph.CreateSpec, len(sddcb.builders))
	nodes := make([]*SysDictDetail, len(sddcb.builders))
	mutators := make([]Mutator, len(sddcb.builders))
	for i := range sddcb.builders {
		func(i int, root context.Context) {
			builder := sddcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SysDictDetailMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sddcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sddcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sddcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (sddcb *SysDictDetailCreateBulk) SaveX(ctx context.Context) []*SysDictDetail {
	v, err := sddcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
